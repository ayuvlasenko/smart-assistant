# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "node"

[setup]
script = '''
set -euo pipefail

cd "${CODEX_WORKTREE_PATH:?CODEX_WORKTREE_PATH is required}"

if [ -n "${CODEX_SOURCE_TREE_PATH:-}" ] && [ -d "$CODEX_SOURCE_TREE_PATH" ]; then
  cp -p "$CODEX_SOURCE_TREE_PATH"/.env* "$CODEX_WORKTREE_PATH"/ 2>/dev/null || true
  [ ! -f "$CODEX_SOURCE_TREE_PATH/mise.local.toml" ] || \
    cp -p "$CODEX_SOURCE_TREE_PATH/mise.local.toml" "$CODEX_WORKTREE_PATH/mise.local.toml"
fi

mise trust
mise install
mise exec -- npm install
'''
